import React from "react";

function Alumni(props) {
    return (
        <section className="alumni">
            <h3 className="section-title">Past Officers and Alumni</h3>
            {/* get the past officers data from firebase and for each year create a dropdown that will
            // contain the past officers and alumni with their linkedIn? */}
        </section>
    )
}

export default Alumni